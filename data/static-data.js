// data/static-data.js

// 2026 (provisorio): solo enero cargado.
export const labelsMeses = ['Ene'];

// CRPC por mes
export const dataCRPC = [837];

// Se deja export para compatibilidad (aunque el gráfico no se muestra por ahora).
export const dataObleasTotales = [0];

// Impacto mensual (%)
export const impactoCRPC = [16.4];

// Se deja export para compatibilidad (aunque el gráfico no se muestra por ahora).
export const impactoObleas = [0];
